# Quick Start

Run the dashboard locally:

```bash
python -m http.server 8000 -d web
```

Open `http://localhost:8000`.

The dashboard loads an energy-price shock by default. Select another preset in the left panel or edit the shock cards directly.

Run Python validation tests with:

```bash
python -m pytest
```
